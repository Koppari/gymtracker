<p id="notice"><%= notice %></p>

<h1>Exercises</h1>

<table class="table table-hover">
  <thead>
  <tr>
    <th> <%= 'Name' %> </th>
  </tr>
  </thead>

  <tbody>
  <% @exercises.each do |exercise| %>
      <tr>
        <td><%= link_to exercise.name, exercise %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to('Create an exercise', new_exercise_path) if current_user %>
